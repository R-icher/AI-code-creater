spring:
  application:
    name: ai-code-creater
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.ryy.aicodecreater.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
